// Placeholder — replaced by `supabase gen types typescript` output once the
// schema is migrated in Stage 2. Keeping `Database = any` here means the
// Supabase client stays untyped (but functional) until then.
// eslint-disable-next-line @typescript-eslint/no-explicit-any
export type Database = any;
